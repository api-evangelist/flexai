{
  "opencollection": "1.0.0",
  "info": {
    "name": "FlexAI Token Factory Audio Embeddings API",
    "version": "v1"
  },
  "request": {
    "auth": {
      "type": "bearer",
      "token": "{{bearerToken}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "Embeddings",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Create embeddings",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://tokens.flex.ai/v1/embeddings",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Generate vector embeddings for the given input using an open embedding model. Billed per token."
        }
      ]
    }
  ],
  "bundled": true
}